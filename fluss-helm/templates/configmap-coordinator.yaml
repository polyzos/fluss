apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fluss.fullname" . }}-coordinator-config
  labels:
    {{- include "fluss.labels" . | nindent 4 }}

data:
  server.yaml: |-
    {{- toYaml .Values.coordinator.config | nindent 4 }}
